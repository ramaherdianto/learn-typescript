"use strict";
describe('Data Type', function () {
    it('should must declare', function () {
        let name = 'Ramadhan Herdianto';
        let balance = 2500000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
